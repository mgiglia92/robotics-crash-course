/*
 * 0-random-longs.ino -- generate random longs
 *
 * Using the random() function, populate a global array of 16 longs and
 * print their values ONCE over UART.  The random values should be in
 * the domain [0, 2147483648].
 *
 * Run the program multiple times by reseting your Arduino, see if you
 * notice anything interesting in the output ;)
 */

// initialize a global array of 16 longs here

void setup()
{
  // initialize UART hardware here

  // loop through the array and assign random values here
}

void loop()
{
  // print the values of the generated array, though you might want to
  // think of a way to only allow for the values to be printed once!
}
